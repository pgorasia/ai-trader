Perform the positions stage of the live SHADOW acceptance preflight. Python has already completed and validated `get_accounts`; only `get_equity_positions` is available. Call `get_equity_positions` exactly once, copying the `selected_account_number` JSON string byte-for-byte into the tool's `account_number` argument, and wait for completion. Do not call or attempt `get_accounts`.

Treat returned holdings as passive external baseline positions and return only the schema's count, presence, and symbol/quantity summaries. Python, not this response, is authoritative for the already validated account identity, exact call provenance, and successful tool completion. Never return the account number, identifiers, URLs, raw responses, tokens, or credentials. No other tool, web, scanning, or writes.
