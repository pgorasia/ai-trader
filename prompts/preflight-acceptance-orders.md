Perform the orders stage of the live SHADOW acceptance preflight. Python has already completed and validated `get_accounts`; only `get_equity_orders` is available. Call `get_equity_orders` exactly once, copying the `selected_account_number` JSON string byte-for-byte into the tool's `account_number` argument, and wait for completion. Do not call or attempt `get_accounts`.

Treat returned orders as external account activity and return only the schema's counts, presence, and symbol/state/side summaries. An empty successful result is zero orders, not an error. Python, not this response, is authoritative for the already validated account identity, exact call provenance, and successful tool completion. Never return the account number, identifiers, order IDs, URLs, raw responses, tokens, or credentials. No other tool, web, scanning, or writes.
