You are performing the dedicated live Luna-schema historical acceptance probe in SHADOW mode. Return only JSON conforming to the supplied Luna cycle schema. Do not read files, use local tools, search the web, or use any MCP server other than the one approved Robinhood historical tool.

Make exactly one `get_equity_historicals` call for the supplied `probe_symbol`. Request regular-session 5-minute OHLCV for the supplied historical exchange session. Never request a literal 15-minute interval. The probe cannot succeed without this observed read; do not substitute, estimate, fabricate, repair, normalize, or reorder bars.

Return exactly one finalist for `probe_symbol`. Copy 3 through 24 source bars from the historical response into `completed_5m_bars`, unchanged, in strict chronological order. Every bar must belong to the supplied session, be five-minute aligned, have `complete=true`, and be fully completed by session close. Include at least one complete aligned group beginning at a 15-minute boundary and containing the consecutive +0, +5, and +10 minute bars, so Python can exercise deterministic 15-minute aggregation.

Use these fixed schema-safe values for fields unrelated to the probe: `cycle_id="luna-schema-live"`; the supplied session date; scanner name/id `historical-acceptance`; `scanner_total=1`; `symbols_processed` containing only the probe symbol; finalist classification `NEW`; numeric finalist metrics `0`; `technical_reason="Historical schema acceptance probe"`; null material requalification; security available/boundary true/no forbidden tools; reconciled account false with zero counts and empty orders; tool counts total 1/run_scan 0; empty errors; and false escalation. Set the top-level timestamp to the supplied market close.

If the historical read fails or valid source bars are unavailable, report the failure in `errors` without inventing data. This will intentionally fail acceptance because the required finalist source-bar contract cannot then be validated.
