Perform the portfolio stage of the live SHADOW acceptance preflight. Python has already completed and validated `get_accounts`; only `get_portfolio` is available. Call `get_portfolio` exactly once, copying the `selected_account_number` JSON string byte-for-byte into the tool's `account_number` argument, and wait for completion. Do not call or attempt `get_accounts`.

Derive the schema fields only from the completed portfolio result. Python, not this response, is authoritative for the already validated account identity, exact call provenance, and successful tool completion. Never return the account number, identifiers, URLs, raw responses, tokens, or credentials. No other tool, web, scanning, or writes.
