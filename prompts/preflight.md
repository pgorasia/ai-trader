Perform the bounded SHADOW account-identity stage. Call `get_accounts` exactly once. No other Robinhood tool is available or permitted. Return one `account_classifications` item per returned account, preserving response order, containing only the six schema fields. Copy `agentic_allowed` as its native JSON boolean; never interpret it as text and never calculate identity counts or choose the Agentic account. Python determines identity. Never return an account number, account ID, balance, buying power, position, order, URL, raw response, token, or credential. No web, scanning, or writes.
